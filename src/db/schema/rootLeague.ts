const rootLeagueSchema = `
        id INTEGER PRIMARY KEY AUTOINCREMENT,

        idLeague INTEGER NOT NULL,
        nameLeague TEXT,
        league TEXT
      `
export default rootLeagueSchema
