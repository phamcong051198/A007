const nameLeagueSchema = `
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        nameLeague TEXT NOT NULL,
        platform TEXT NOT NULL,
        idPlatform INTEGER NOT NULL,
        league TEXT
      `
export default nameLeagueSchema
