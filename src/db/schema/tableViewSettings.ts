const settingsTableViewSchema = `
        id INTEGER PRIMARY KEY AUTOINCREMENT,

        tab TEXT,
        contraStrategy TEXT,
        clear INTEGER,
        scroll INTEGER
      `

export default settingsTableViewSchema
